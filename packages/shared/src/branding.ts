/**
 * Единственный источник бренда. Переименование продукта = правка этого файла.
 * Ничто в кодовой базе не должно упоминать имя продукта напрямую.
 */
export const BRAND = {
  /** Машинное имя: имена конфигов, каталогов, процессов. Только [a-z0-9-]. */
  id: 'slojka',
  /** Отображаемое имя (основной язык). */
  name: 'Слойка',
  /** Отображаемое имя латиницей (пакеты, файлы, en-интерфейс). */
  nameLatin: 'Slojka',
  /** Расширение файла проекта, с точкой. */
  fileExt: '.slojka',
  /** Mimetype файла проекта (пишется в zip первым файлом, как в OpenRaster). */
  mimetype: 'image/x-slojka',
  version: '0.1.0',
} as const

export type Brand = typeof BRAND
